# Метаданные: HTTP-сервисы

Строк метаданных: **7**.

| № | Тип | Техническое имя | Синоним | Технический путь | Подсистемы | Функциональные опции | Ссылка метаданных | Источник XML |
|---:|---|---|---|---|---|---|---|---|
| 1 | HTTP-сервис | edo | Edo | Конфигуратор → Конфигурация → HTTP-сервисы → edo | Конфигуратор → Конфигурация → Подсистемы → ЭлектронноеВзаимодействие → Подсистемы → ЦентрЭДО | — | HTTPService.edo | HTTPServices/edo.xml |
| 2 | HTTP-сервис | exchange_dsl_1_0_0_1 | Exchange DSL 1.0.0.1 | Конфигуратор → Конфигурация → HTTP-сервисы → exchange_dsl_1_0_0_1 | Конфигуратор → Конфигурация → Подсистемы → СтандартныеПодсистемы → Подсистемы → ОбменДанными → Подсистемы → ТранспортСообщенийОбмена | — | HTTPService.exchange_dsl_1_0_0_1 | HTTPServices/exchange_dsl_1_0_0_1.xml |
| 3 | HTTP-сервис | Биллинг | Биллинг | Конфигуратор → Конфигурация → HTTP-сервисы → Биллинг | Конфигуратор → Конфигурация → Подсистемы → ТехнологияСервиса → Подсистемы → ОплатаСервиса | — | HTTPService.Биллинг | HTTPServices/Биллинг.xml |
| 4 | HTTP-сервис | ОткрытаяАвторизацияПочтовогоСервиса | Открытая авторизация почтового сервиса | Конфигуратор → Конфигурация → HTTP-сервисы → ОткрытаяАвторизацияПочтовогоСервиса | Конфигуратор → Конфигурация → Подсистемы → СтандартныеПодсистемы → Подсистемы → РаботаСПочтовымиСообщениями | — | HTTPService.ОткрытаяАвторизацияПочтовогоСервиса | HTTPServices/ОткрытаяАвторизацияПочтовогоСервиса.xml |
| 5 | HTTP-сервис | ПередачаДанных | Передача данных | Конфигуратор → Конфигурация → HTTP-сервисы → ПередачаДанных | Конфигуратор → Конфигурация → Подсистемы → ТехнологияСервиса → Подсистемы → ПередачаДанных | — | HTTPService.ПередачаДанных | HTTPServices/ПередачаДанных.xml |
| 6 | HTTP-сервис | СерверЛояльности | Сервер лояльности | Конфигуратор → Конфигурация → HTTP-сервисы → СерверЛояльности | Конфигуратор → Конфигурация → Подсистемы → СерверЛояльности → Подсистемы → ПоставщикДанных | — | HTTPService.СерверЛояльности | HTTPServices/СерверЛояльности.xml |
| 7 | HTTP-сервис | ЭДО | ЭДО | Конфигуратор → Конфигурация → HTTP-сервисы → ЭДО | Конфигуратор → Конфигурация → Подсистемы → ЭлектронноеВзаимодействие → Подсистемы → ОбменСКонтрагентами → Подсистемы → СинхронизацияСлужебный → Подсистемы → СервисЭДОСлужебный | — | HTTPService.ЭДО | HTTPServices/ЭДО.xml |
